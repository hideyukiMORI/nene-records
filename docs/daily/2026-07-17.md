# 2026-07-17

## フッター社名ブランドの / リンク化（DB内容変更・施主指示）

- 施主「トップへ行く導線がヘッダーロゴだけ。フッター社名ロゴにも / リンクを」→ 実装。
- ayane 13ページのフッターブランド外側 `<div style="display:flex;…;flex-wrap:wrap">` を**同一インラインスタイルの `<a href="/" …;text-decoration:none>`** に置換（開き/閉じの2アンカーとも全13ページで各1ヒットを事前確認・バックアップ `nene_records_pre_footer_link_20260717-0004.sql.gz`・置換13/残0）。
- 実測（Playwright・本番）: レイアウト完全同一（flex・gap14px・下線なし・IMG+SPAN+SPAN）・クリックで `/` へ SPA 遷移（#885 委譲リスナが拾い #921 クロスフェードも適用）。
- ★bespoke chrome の教訓再掲: この種の全ページ共通変更は DB 一括 REPLACE が正（納品アーカイブは古いまま＝再投入で退行、DB が正本）。

## hub 追補（07-16 深夜起草・07-17 着弾）との突合

- 追補1「/blog 判断待ち継続」→ **完了済み**（07-16 施主裁定「一次的に固定ページ」・entity 1150・本番 200）
- 追補3「for-accountants リンク除去 即可」→ **完了済み**（07-16 全除去・監査で迷子リンク0を再確認）
- 追補の #50/#896 → **クローズ済み**（PDF は media 配信が canonical・Tier A 移送は zip 系統 #798 が media 原本を運ぶ）
- 残: exemplar アンカー errors.ts 1本（fleet-tooling の回答待ち・指示書どおり待機）

---

## NENE2 表記のリポリンク化（DB内容変更・施主GO）

- 施主「NENE2 の部分、GitHub リポ URL にリンクした方がいい」→ 調査で**より重要な発見**: /products 証拠棚の「NENE2 は公開フレームワーク →」カードのリンク先が **profile で、リポ本体でなかった**（主張と証拠の不一致）。
- 実施（5箇所・ページ毎スコープの REPLACE・置換数 1/1/1/1 検証済み）: カードのリンク先是正＋各ページ初出（/・/products・/services・/company）の文中リンク化。既存外部リンクの作法踏襲（明色 bg=#D64525・暗色 bg=#E0603F・下線なし bold）。2回目以降の出現・JSON-LD/メタは非対象。リポは公開・description 整備済み・07-14 更新を事前確認。
- ★教訓（30分ハマり候補だった）: **この DB の text_fields を mysql クライアント経由で REPLACE するときは `SET NAMES utf8mb4` 必須**。無いと日本語リテラルが charset 変換で不一致になり REPLACE が silent no-op（LIKE '%NENE2%' など ASCII は当たるので気づきにくい）。07-16 の footer 置換が通ったのは CHAR(10) 連結と部分 ASCII アンカーの偶然。以後の内容変更 SQL はテンプレ先頭に SET NAMES を固定。
- ★もう1つ: DB 内の bespoke html は**クォートが `&#039;` エンティティ**（sanitizer 出力）。rendered HTML からアンカー文字列を採ると不一致になる — 置換文字列は必ず DB 生値（SUBSTRING）から採る。
- README が見込み客から1クリックになった件は統合リナへ相談中（(a) NENE2 リナ依頼 /(b) hub 裁定 /(c) records で read-only 監査、推し=(c)→(a)）。

---

## 事実訂正の台帳反映: ayane.co.jp は「現役」（hub 訂正共有・records 実測で裏取り）

- **正**: `ayane.co.jp` は **200・旧会社サイトが現役**（実測: `<title>彩音インターナショナル株式会社 : イラストレーション システム開発</title>`）。「未移行」なのは**新サイト**（現在は ayane.nene-records.com で稼働）であって、**ドメインは未稼働ではない**。
- 帰結: NENE2 README の AYANE 逆リンクは**恒久ドメイン ayane.co.jp で確定**（DNS 切替後も張り替え不要・どの時点でも 404 にならない）。records が nene2 リナに送った「移行時に更新対象」という申し送りは**不要になった**（撤回済み扱い）。
- ⚠️ 語の規律: 「ayane.co.jp 未稼働」という表現は DNS 切替計画の判断を誤らせるため使わない。正しくは「旧サイト稼働中・新サイトは records 上・切替待ち」。
