# 2026-07-17

launch（07-19・撮影後 GO）待機日を「go-live 依存ゼロ」まで詰めた日。プリステージで移送経路の欠陥（PHP zip 拡張欠落）を事前検出して修正・本番 export リハ成功、フッター社名リンク／SNS リンク／NENE2 リポリンク／コピー事実整合／トップ3カード誠実化／/contact 実フォーム化を DB 変更と2デプロイ（第41〜42回・#933/#940）で出荷した。SanitizedHtml の target 剥ぎという SSR/SPA parity バグ（#939）の発見・修正が最大の拾い物。

## フッター社名ブランドの / リンク化（DB内容変更・施主指示）

- 施主「トップへ行く導線がヘッダーロゴだけ。フッター社名ロゴにも / リンクを」→ 実装。
- ayane 13ページのフッターブランド外側 `<div style="display:flex;…;flex-wrap:wrap">` を**同一インラインスタイルの `<a href="/" …;text-decoration:none>`** に置換（開き/閉じの2アンカーとも全13ページで各1ヒットを事前確認・バックアップ `nene_records_pre_footer_link_20260717-0004.sql.gz`・置換13/残0）。
- 実測（Playwright・本番）: レイアウト完全同一（flex・gap14px・下線なし・IMG+SPAN+SPAN）・クリックで `/` へ SPA 遷移（#885 委譲リスナが拾い #921 クロスフェードも適用）。
- ★bespoke chrome の教訓再掲: この種の全ページ共通変更は DB 一括 REPLACE が正（納品アーカイブは古いまま＝再投入で退行、DB が正本）。

## hub 追補（07-16 深夜起草・07-17 着弾）との突合

- 追補1「/blog 判断待ち継続」→ **完了済み**（07-16 施主裁定「一次的に固定ページ」・entity 1150・本番 200）
- 追補3「for-accountants リンク除去 即可」→ **完了済み**（07-16 全除去・監査で迷子リンク0を再確認）
- 追補の #50/#896 → **クローズ済み**（PDF は media 配信が canonical・Tier A 移送は zip 系統 #798 が media 原本を運ぶ）
- 残: exemplar アンカー errors.ts 1本（fleet-tooling の回答待ち・指示書どおり待機）

---

## NENE2 表記のリポリンク化（DB内容変更・施主GO）

- 施主「NENE2 の部分、GitHub リポ URL にリンクした方がいい」→ 調査で**より重要な発見**: /products 証拠棚の「NENE2 は公開フレームワーク →」カードのリンク先が **profile で、リポ本体でなかった**（主張と証拠の不一致）。
- 実施（5箇所・ページ毎スコープの REPLACE・置換数 1/1/1/1 検証済み）: カードのリンク先是正＋各ページ初出（/・/products・/services・/company）の文中リンク化。既存外部リンクの作法踏襲（明色 bg=#D64525・暗色 bg=#E0603F・下線なし bold）。2回目以降の出現・JSON-LD/メタは非対象。リポは公開・description 整備済み・07-14 更新を事前確認。
- ★教訓（30分ハマり候補だった）: **この DB の text_fields を mysql クライアント経由で REPLACE するときは `SET NAMES utf8mb4` 必須**。無いと日本語リテラルが charset 変換で不一致になり REPLACE が silent no-op（LIKE '%NENE2%' など ASCII は当たるので気づきにくい）。07-16 の footer 置換が通ったのは CHAR(10) 連結と部分 ASCII アンカーの偶然。以後の内容変更 SQL はテンプレ先頭に SET NAMES を固定。
- ★もう1つ: DB 内の bespoke html は**クォートが `&#039;` エンティティ**（sanitizer 出力）。rendered HTML からアンカー文字列を採ると不一致になる — 置換文字列は必ず DB 生値（SUBSTRING）から採る。
- README が見込み客から1クリックになった件は統合リナへ相談中（(a) NENE2 リナ依頼 /(b) hub 裁定 /(c) records で read-only 監査、推し=(c)→(a)）。

---

## 事実訂正の台帳反映: ayane.co.jp は「現役」（hub 訂正共有・records 実測で裏取り）

- **正**: `ayane.co.jp` は **200・旧会社サイトが現役**（実測: `<title>彩音インターナショナル株式会社 : イラストレーション システム開発</title>`）。「未移行」なのは**新サイト**（現在は ayane.nene-records.com で稼働）であって、**ドメインは未稼働ではない**。
- 帰結: NENE2 README の AYANE 逆リンクは**恒久ドメイン ayane.co.jp で確定**（DNS 切替後も張り替え不要・どの時点でも 404 にならない）。records が nene2 リナに送った「移行時に更新対象」という申し送りは**不要になった**（撤回済み扱い）。
- ⚠️ 語の規律: 「ayane.co.jp 未稼働」という表現は DNS 切替計画の判断を誤らせるため使わない。正しくは「旧サイト稼働中・新サイトは records 上・切替待ち」。

---

## コピーの事実整合修正: 「このサイトのイラストも自前」を撤去（DB内容変更・施主GO）

- 施主の違和感「リニューアルサイトにイラストあったっけ」→ 全ページ画像実測: **イラスト0・図版0**（画像はロゴ＋製品スクショ8枚のみ）。トップ証拠棚カードと /services カードの2箇所が**検証すると偽になる主張**だった（他3カードは GitHub 証拠リンク付きなのに、この1枚だけ実物なし）。
- 「イラスト」言及の全数調査（DB 正本）: 偽は上記2箇所のみ。他（国際巡回展・桜Exhibition・イラストレーション制作・デザイン単体相談の案内）は**実在の事業記述なので不変**。
- 修正（バックアップ `nene_records_pre_illust_copy_20260717-0110.sql.gz`）: 見出し「UI・画面デザイン**・図版**まで内製」→「UI・画面デザインまで内製」（両ページ・(外注なし) 変種含む）／添え書き →「**このサイト自体も自前で作っています**」（/services は「当社で作っています。」）— 真であり「このサイトが実物サンプル」の含みが出て主張としてむしろ強い。
- 将来: 第2弾で実物イラストがサイトに入ったら「イラストも自前」を復活可（IP 事業と接続すれば本物の差別化）。live 検証済み・残存0。

---

## launch プリステージ＋並行タスク一括（07-17 午前〜）

### プリステージ（施主方針: 撮影後に launch 実行・GO まで即応準備）

- 🔴 **移送経路の欠陥を事前検出**: 本番 app コンテナに PHP zip 拡張が無く `export-org.php` が ZipArchive not found（#932/PR#933・**第41回デプロイ**で修正。※初回デプロイ実行がツール障害で中断→状態確認で未完了を検出→再実行、の学び: **デプロイは「実行した」でなく「コンテナで確認した」まで**）。修正後、**本番 export リハ成功**（ayane org・1.88MB zip・media 3 同梱）。
- **runbook 追補**（_work/ayane-tier-a-install-runbook.md）: ドメイン確定 ayane.co.jp／設置物は **GO 時に v0.5.3 を切る**推奨（0.5.2 に launch 品質 #876〜#933 が無い）／301 候補マップ（旧サイト全81 URL 実測→パターン別）／GO 当日チェックリスト。

### トップ「最近の動き」3カードの誠実化（施主 B案 GO・DB変更・反映済み）

- 実記事1本なのに3投稿に見える問題。invoice デモカード「続きを読む→/blog」が**実記事なしの行き止まり**だった → **「デモを触る →」実デモ直リンク**へ。shop カードは「お知らせを見る →」に（記事感除去）。renewal カードは実記事ありでそのまま。live 検証済み・誤爆なし。

### /contact 実フォーム化（hub 裁定 = C 先行・A は launch 後）

- 実測で確定した制約: bespoke html の sanitizer は **script/iframe/form/input を全除去**（PHP 実証・DOMPurify 双子）＝ embed.js は直接貼れない。
- (C) リンクアウト: contact 側が単独フォームページを新設中（GO 済み・URL 待ち）。**事前実測済み**: sanitizer は `target="_blank" rel="noopener"` を保持・SPA 委譲リスナは target 付きを除外＝新タブがネイティブに開く。
- (A) 正式版 = **#937 起票済み**（trusted-embed の sanitizer 延長・origin+SRI 完全一致・本番確定値焼き込み・launch 後）。

### 規約系（hub 代行確認を受け実施）

- exemplar アンカー errors.ts 1本 = **PR #936**（綴りの正本を fleet-tooling 実装 exemplars.ts:41-42 で裏取り・コメント1行のみ・check 緑・**指示書どおりマージせず**施主/hub 待ち）。

---

## 夜バッチ: SNS フッター・target parity 修正・/contact 実フォーム化（すべて本番反映済み）

### フッター SNS リンク（施主 GO・矢印なし・gap 32px）

- footer_config に登録済みの X/YouTube/Facebook が **bare では表示されない**（PublicSiteShell のみ描画）という施主指摘 → bespoke フッターのコピーライト行を左右分割し、右側にモノスペースのテキストリンク3本を追加（13ページ一括・新タブ・登録値そのまま）。

### 🔴 #939/PR#940 — SanitizedHtml の target 剥ぎ = SSR/SPA parity バグ（launch 前の拾い物）

- SNS リンクの live 検証で発見: **SSR（PHP sanitizer）は target 保持・SPA（DOMPurify）は剥ぐ** → ハイドレーションで新タブリンクが同タブに退化。「文書化された仕様」でも twins が食い違えば parity 違反（#891 型）。
- 修正 = `ADD_ATTR: ['target']` ＋ afterSanitizeAttributes フックで **target 付きアンカーに noopener/noreferrer 強制**（攻撃面と引き換えの許可はしない）。**第42回デプロイ**・SNS リンク3本と /contact ボタンの両面で target/rel を本番実測。

### /contact 実フォーム化（(C) 完了・hub 確認済み）

- contact リナが単独フォームページを新設（https://contact.ayane.co.jp/form/ayane-contact・contact#383）→ records は PLACEHOLDER 箱（3,265B・div深度でスパン確定）を CTA パネル「お問い合わせフォームを開く」（新タブ）に置換。
- **事実整合も同時に**: 実フォームは3項目＋honeypot（実測）なので、存在しない「会社名・屋号/種類は任意」の記述を削除。準備中文言も撤去。
- Playwright E2E: クリック→新タブで実フォーム表示・元ページ残留・target/rel 完備。contact の「フォント Times」報告は contact 側の測定ミス訂正で対応不要と確定。

### プロセス違反の記録（自己申告・hub 裁定 (a) 維持）

- exemplar アンカー（#935/PR#936・「マージしない」指定）を、**docs ブランチの切り忘れ**（exemplar ブランチから分岐）で PR#938 に同乗させ main 反映してしまった。変更は事前承認済み1行・CI緑・実害なし。hub 裁定 = このまま・#936 superseded クローズ・revert 不要（churn だけのプロセス劇場を避ける）。**再発防止 = ブランチ作成は必ず `git checkout main && git pull` から**（本バッチから適用）。

### Notion TODO 同期

- 「AYANE サイト 構成監査 & TODO」の /contact・3カード整合を消し込み。launch 前の残りは og:image のみ。

---

## 📊 本日の数字

- デプロイ: 第41回（#933 zip 拡張）・第42回（#940 target parity）〔実測・runbook 準拠・全緑〕
- DB 内容変更: 5件（フッター/リンク・SNS・カード・コピー・contact）— すべてバックアップ＋事前一意性確認＋live 検証〔実測〕
- Issue/PR: #926〜#941（docs 同期含む）。exemplar #936 は superseded クローズ（hub 裁定〔伝聞〕・経緯は本文）
- 本番 export リハ: ayane org 1,882,970 bytes・media 3 同梱〔実測〕

---

## AYANE 代表写真3枠の本番差し込み（DB内容変更・施主支給・#943）

- 施主がプロ撮影（Photoproduce・07-17）から `business_portrait_3x4.jpg`（**1913×2550・3:4・sRGB・817KB**）を支給 → 事前指定仕様（長辺2000〜2560・10MB未満）に適合を実測確認。
- **media 投入（org=ayane・id=4）**: 実装と同じ命名（`random_bytes(16)` hex）で `var/media/2026/07/0c6103a…jpg` に配置（www-data 所有）＋ media 行 INSERT（width/height/alt 記録）。**原本 200（837KB）・lg 派生オンデマンド生成 200（152KB）を本番実測**。media volume は 07-12 恒久化済みを mount 実測で再確認。
- **差し込み3枠**（外側枠 div は不変・stripe 背景は読み込み背景として残置・内側の丸アイコン＋キャプションのみ `<img>` 置換）:
  | 枠 | 場所 | アスペクト | object-position |
  |---|---|---|---|
  | hero | `/home` | 16:7 | 50% 8%（12%→頭頂切れで微調整） |
  | 体制と代表 | `/home` | 5:4 | 50% 15% |
  | 代表プロフィール | `/company/ceo` | 4:5 | 50% 25% |
- ★**sanitizer 制約の事前実測が効いた**: PHP twin（`PublicHtmlSanitizer`）の img 許可属性は `src/alt/title/width/height`＋`style` のみ。`srcset/sizes/loading` は SSR で剥がれ SPA と食い違う（#939 型 parity 破れ）ため**最初から使わない**構成（lg 1280 幅単発・style で cover/position）を選択。
- 手順の作法: 置換文字列は DB 生値から採取（`&#039;` エンティティ対応）・`SET NAMES utf8mb4`・div 深度パーサで枠の内側だけを外科的置換・「施主支給予定/プロ撮影」残置 0 を機械確認。バックアップ = `~/nene_records_pre_portrait_20260717.sql.gz`（text_fields 1137/1141）。
- 検証: SSR で 3 img が属性完全出力（curl・browser Accept）・Playwright 実ブラウザ（hydration 後）で3枠の顔位置を目視確認。コード変更・デプロイなし（DB 内容変更のみ）。
- これで施主手番の「顔写真=撮影後」は解消。残る施主手番 = Tier A 移送/DNS・#912 GO。

---

## AYANE 代表写真の施主トリミング確定＋680px ブレイク導入（DB内容変更・#945）

- #943 の初回差し込み後、施主レビューの反復で /home の2枠を**施主トリミング原版そのまま**に確定:
  - **hero**: 枠 16/7→**3/2**（右セル 548px ≒ 左カラム 539px・施主の「左とのバランス」指摘を実測で裏付け）。
    施主支給 1913×1275（media id=5）を無加工表示。途中「頭上余白」をマスター+object-position で作る版を
    挟んだが、指摘対象は下段の誤解と判明し**施主トリミング原版に復帰**。
  - **体制と代表**: 施主から縦 1530×1913 が届き 4:5 化を一度実施 → 真意は「横長維持・**620px 幅の上下余白
    解消**」→ 5:4 へ復帰し、再支給の 1913×1530（media id=7）で確定。未使用の縦版 media id=6 は
    行・実体・**派生キャッシュ（derivatives/lg/{avif,jpeg}）**まで削除（HTTP 404 実測）。
- **≈680px 縦積みブレイク**（メディアクエリ不可の bespoke 制約下・intrinsic 方式）:
  - hero: flex basis 440/300→**350/240**（grow 1.4:1 が幅を支配するためデスクトップ実寸不変 627/438）。
  - 体制: grid minmax **240→300px**（このセクションのグリッドだけをアンカー特定・他の 240px grid は不変）。
  - 実測: 1280/700 = 2カラム・660/620 = 縦積み。620px で「写真の上下に空白 ~230px」（2カラム残存＋
    align-center が原因）が解消。
- 作法: 全変更フル値 UPDATE＋事前一意性 assert・バックアップ3点（pre_hero32/pre_hero_break/pre_team45）・
  Playwright 4幅実測。★LIKE 検証に `%` を含む文字列（object-position 値）を使うとワイルドカード誤爆する
  — 検証は Python 側 count/assert が正。
- media 最終状態: id=4 マスター（/company/ceo 使用）・id=5 hero・id=7 体制（+PDF 3点）。
