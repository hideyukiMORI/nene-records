{
  "name": "nene-records-mcp-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin remote MCP server that bridges docs/mcp/tools.json to the NeNe Records HTTP API (for Claude.ai custom connectors).",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2"
  }
}
